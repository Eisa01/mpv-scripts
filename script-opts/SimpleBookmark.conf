######----Settings For SimpleBookmark 1.0----######
####------Script Settings-----####

##--Runs automatically when opening mpv and there is no video / file loaded. none for disabled. Or choose between: all, slots, protocols, fileonly, titleonly, timeonly, keywords.
auto_run_list_idle=none

##--When attempting to bookmark, if there is no video / file loaded, it will instead jump to your last bookmarked item
bookmark_loads_last_idle=yes

##--When quick saving a bookmark to keybind slot, it will not save position
slots_quicksave_fileonly=yes

##--If the keybind slot is empty, this enables quick bookmarking and adding to slot, Otherwise keybinds are assigned from the bookmark list or via quicksave.
slots_empty_auto_create=no

##--When auto creating slot, it will not save position.
slots_empty_fileonly=yes

##--When loading a slot, it will auto resume to the bookmarked time.
slots_auto_resume=yes

##--Show file paths instead of media-title
show_paths=no

##--change to 0 so that bookmark resumes from the exact position, or decrease the value so that it gives you a little preview before loading the resume point
resume_offset=-0.65

##--yes is for displaying osd messages when actions occur. Change to no will disable all osd messages generated from this script
osd_messages=yes

##--yes is for going up on the first item loops towards the last item and vise-versa. no disables this behavior.
loop_through_list=no

##--the default sorting method for the bookmark list. select between "added-asc", "added-desc", "alphanum-asc", "alphanum-desc"
list_default_sort=added-asc

##--yes is for marking the bookmarked time as a chapter. no disables mark as chapter behavior.
mark_bookmark_as_chapter=no
 
##--To smartly set the search as not typing (when search box is open) without needing to press ctrl+enter.
search_not_typing_smartly=yes

####------Filter Settings-------####
##---available filters: "all" to display all the bookmarks. Or "slots" to display the bookmarks filtered with slots. Or "fileonly" to display files saved without time. Or "timeonly" to display files that have time only. Or "keywords" to display files with matching keywords specified in the configuration. Or "playing" to show bookmarks for current playing file.
##---available sort: "added-asc" is for the newest added bookmark to show first. Or "added-desc" for the newest added to show last. Or "alphanum-asc" is for A to Z approach with filename and episode number lower first. Or "alphanum-desc" is for its Z to A approach. Or "keybind-asc", or "keybind-desc" to sort based on keybinds which is execlusive to sort_slots_filter.

##--Jump to the following filters and in the shown sequence when navigating via left and right keys. You can change the sequence and delete filters that are not needed.
filters_and_sequence=["all", "slots", "protocols", "fileonly", "titleonly", "timeonly", "playing", "keywords"]

##--Create a filter out of your desired "keywords", e.g.: youtube.com will filter out the videos from youtube. You can also insert a portion of filename or title, or extension or a full path / portion of a path.
keywords_filter_list=["youtube.com", "mp4", "naruto", "c:\\users\\eisa01\\desktop"]

##--Sorts the slots filter. Select between none, keybind-asc, keybind-desc, added-asc, added-desc, alphanum-asc, alphanum-desc. description: none is for default ordering. keybind-asc is only for slots, it uses A to Z approach but for keybinds. keybind-desc is the same but for Z to A approach.
sort_slots_filter=keybind-asc

##--Sorts the filter. Select between "none", "added-asc", "added-desc", "alphanum-asc", "alphanum-desc".
sort_fileonly_filter=alphanum-asc
sort_protocols_filter=none
sort_titleonly_filter=none
sort_timeonly_filter=none
sort_keywords_filter=none
sort_playing_filter=none
sort_search_filter=none

##--yes is for bypassing the last filter to go to first filter when navigating through filters using arrow keys, and vice-versa. no disables this behavior.
loop_through_filters=yes

####------Logging Settings------####
 
##--Change to /:dir%script for placing it in the same directory of script, OR change to /:dir%mpvconf for mpv portable_config directory OR specify the desired path, e.g.: C:\Users\Eisa01\Desktop\
log_path=/:dir%mpvconf

##--Name+Extension of the file that will be used to store the log data
log_file=mpvBookmark.log

##--Date format in the log (see lua date formatting), e.g.:"%d/%m/%y %X" or "%d/%b/%y %X"
date_format=%d/%m/%y %X

##--The text that is stored for the video time inside log file. It can also be left blank.
bookmark_time_text=time=

##--The text that is stored for the keybind slot inside log file. It can also be left blank.
keybind_slot_text=slot=

##--Change between all, protocols, none. This option will store the media title in log file, it is useful for websites / protocols because title cannot be parsed from links alone
file_title_logging=protocols

##--Add below (after a comma) any protocol you want its title to be stored in the log file. This is valid only for (file_title_logging="protocols" or file_title_logging="all")
protocols=["https?://", "magnet:", "rtmp:"]

##--Prefers to use filename over filetitle. Select between local, protocols, all, and none. "local" prefer filenames for videos that are not protocols. "protocols" will prefer filenames for protocols only. "all" will prefer filename over filetitle for both protocols and not protocols videos. "none" will always use filetitle instead of filename
prefer_filename_over_title=local


####------Boorkmark Menu Settings------####

##--Text color for list in BGR hexadecimal
text_color=ffffff

##--Font size for the text of bookmark list
text_scale=50

##--Black border size for the text of bookmark list
text_border=0.7

##--Highlight color in BGR hexadecimal
highlight_color=ffbf7f

##--Font size for highlighted text in bookmark list
highlight_scale=50

##--Black border size for highlighted text in bookmark list
highlight_border=0.7

##--Text to be shown as header for the bookmark list. %cursor: shows the position of highlighted file. %total: shows the total amount of bookmarked items. %filter: shows the filter name, %prefilter: user defined text before showing filter, %afterfilter: user defined text after showing filter, %search: shows the typed search, %presearch, %aftersearch: same concept of prefilter and afterfilter.
header_text=🔖 Bookmarks [%cursor/%total] %prefilter%filter%afterfilter%presearch%search%aftersearch

##--Text to be shown before filter in the header
header_filter_pre_text= (filtered: 

##--Text to be shown after filter in the header (since filter is inside the header, if you need to add a variable like %%search it will need double %%)
header_filter_after_text=)

##--Text to be shown before search in the header
header_search_pre_text=\h\N\N(search=

##--Text to be shown after search in the header
header_search_after_text=..)

##--Header color in BGR hexadecimal
header_color=ffffaa

##--Search color when in typing mode
search_color_typing=00bfff

##--Search color when not in typing mode and it is active
search_color_not_typing=ffffaa

##--Header text size for the bookmark list
header_scale=55

##--Black border size for the Header of bookmark list
header_border=0.8

##--Show the number of each bookmark item before displaying its name and values.
show_item_number=yes

##--Change to yes or no. Slices long filenames per the amount specified below
slice_longfilenames=no

##--Amount for slicing long filenames
slice_longfilenames_amount=55

##--Time seperator that will be used after title / filename for bookmarked time
time_seperator= 🕒 

##--Slot seperator that will be used after the bookmarked time
slot_seperator= ⌨ 

##--Change maximum number to show items at once
list_show_amount=10

##--The text that indicates there are more items above. \h\N\N is for new line.
list_sliced_prefix=...\h\N\N

##--The text that indicates there are more items below.
list_sliced_suffix=...

##--no is for more items to show, then u must reach the end. yes is for new items to show after reaching the middle of list.
list_middle_loader=yes

##--Will exit the bookmark list when double tapping the bookmark list, even if the list was accessed through a different filter.
bookmark_list_keybind_twice_exits=yes

####------Keybind Settings------####
##---Add below (after a comma) any additional keybind you want to bind. Or change the letter inside the quotes to change the keybind
##---Example of changing and adding keybinds: --From ["b", "B"] To ["b"]. --From [""] to ["alt+b"]. --From [""] to ["a" "ctrl+a", "alt+a"]

##--Keybind that will be used to display the bookmark list
bookmark_list_keybind=["b", "B"]

##--Keybind that will be used to save the video and its time to bookmark file
bookmark_save_keybind=["ctrl+b", "ctrl+B"]

##--Keybind that will be used to save the video without time to bookmark file
bookmark_fileonly_keybind=["alt+b", "alt+B"]

##--Keybind that will be used to bind a bookmark to a key. e.g.: Press alt+1 on a bookmark slot to assign it when list is open, press while list is hidden to load. (A new slot is automatically created for each keybind. e.g: .."alt+9, alt+0". Where alt+0 creates a new 10th slot.)
bookmark_slots_add_load_keybind=["alt+1", "alt+2", "alt+3", "alt+4", "alt+5", "alt+6", "alt+7", "alt+8", "alt+9"]

##--Keybind that is used to remove the highlighted bookmark slot keybind from a bookmark entry when the bookmark list is open.
bookmark_slots_remove_keybind=["alt+-", "alt+_"]

##--To save keybind to a slot without opening the bookmark list, to load these bookmarks it uses bookmark_slots_add_load_keybind
bookmark_slots_quicksave_keybind=["alt+!", "alt+@", "alt+#", "alt+$", "alt+%", "alt+^", "alt+&", "alt+*", "alt+)"]

##--Keybind that will be used to navigate up on the bookmark list
list_move_up_keybind=["UP", "WHEEL_UP"]

##--Keybind that will be used to navigate down on the bookmark list
list_move_down_keybind=["DOWN", "WHEEL_DOWN"]

##--Keybind that will be used to go to the first item for the page shown on the bookmark list
list_page_up_keybind=["PGUP"]

##--Keybind that will be used to go to the last item for the page shown on the bookmark list
list_page_down_keybind=["PGDWN"]

##--Keybind that will be used to navigate to the first item on the bookmark list
list_move_first_keybind=["HOME"]

##--Keybind that will be used to navigate to the last item on the bookmark list
list_move_last_keybind=["END"]

##--Keybind that will be used to load highlighted entry from the bookmark list
list_select_keybind=["ENTER", "MBTN_MID"]

##--Keybind that will be used to add the selected entry to playlist
list_add_playlist_keybind=["SHIFT+ENTER"]

##--Keybind that will be used to close the bookmark list (closes search first if it is open)
list_close_keybind=["ESC", "MBTN_RIGHT"]

##--Keybind that will be used to delete the highlighted entry from the bookmark list
list_delete_keybind=["DEL"]

##--Keybind that will be used to trigger search
list_search_activate_keybind=["ctrl+f", "ctrl+F"]

##--Keybind that will be used to exit typing mode of search while keeping search open
list_search_not_typing_mode_keybind=["CTRL+ENTER"]

##--Keybind entries from 0 to 9 for quick selection when list is open (list_show_amount=10 is maximum for this feature to work)
quickselect_0to9_keybind=yes

####------Filter Keybind Settings------####

##--Keybind that will be used to go to the next available filter based on the configured sequence
next_filter_sequence_keybind=["RIGHT", "MBTN_FORWARD"]

##--Keybind that will be used to go to the previous available filter based on the configured sequence
previous_filter_sequence_keybind=["LEFT", "MBTN_BACK"]

##--Keybind to jump to the specific filter when list is open
slots_filter_inside_list_keybind=["s", "S"]
fileonly_filter_inside_list_keybind=["f", "F"]
timeonly_filter_inside_list_keybind=[""]
protocols_filter_inside_list_keybind=[""]
titleonly_filter_inside_list_keybind=[""]
keywords_filter_inside_list_keybind=[""]
playing_filter_inside_list_keybind=[""]

##--Keybind to jump to the specific filter when list is closed
slots_filter_outside_list_keybind=["s", "S"]
fileonly_filter_outside_list_keybind=[""]
timeonly_filter_outside_list_keybind=[""]
protocols_filter_outside_list_keybind=[""]
titleonly_filter_outside_list_keybind=[""]
keywords_filter_outside_list_keybind=[""]
playing_filter_outside_list_keybind=[""]

######----End of Settings----######
